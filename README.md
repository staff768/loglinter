### LogLinter

LogLinter — это статический анализатор кода для языка Go, разработанный как плагин для golangci-lint. Он проверяет лог-записи в коде на соответствие правилам оформления и безопасности.

### Основные возможности

Линтер анализирует вызовы популярных логгеров (log/slog, go.uber.org/zap) и проверяет текст сообщений по следующим критериям:

 - **Регистр**: Лог-сообщения должны начинаться со строчной (маленькой) буквы.

 - **Язык**: Допускается использование только английского языка (ASCII символы).

 - **Спецсимволы** и эмодзи: Запрещено использование эмодзи и знаков препинания в конце сообщения (., !, ?).

 - **Безопасность**: Поиск потенциально чувствительных данных (пароли, токены, API-ключи) в тексте логов.

Требования

Go: 1.22+

golangci-lint: v1.62.0+

Структура проекта

pkg/analyzer/ — основная логика линтера на базе go/analysis.

plugin/ — код для интеграции с golangci-lint через Module Plugin System.

testdata/ — наборы тестов для проверки правил.

cmd/loglinter/ — точка входа для использования в качестве самостоятельной утилиты.

### Сборка и запуск

Поскольку проект использует современную систему плагинов, вам необходимо собрать кастомную версию golangci-lint, включающую этот линтер.

1. Подготовка

Убедитесь, что у вас установлен golangci-lint:

```sh
go install github.com/golangci/golangci-lint/cmd/golangci-lint@latest
```

2. Сборка кастомного бинарника

В корне проекта выполните команду:

# Загрузка зависимостей

```sh
go mod tidy
```

# Сборка кастомного линтера

```sh
golangci-lint custom
```

После завершения в папке появится файл custom-gcl (или custom-gcl.exe в Windows).

3. Настройка конфигурации

Линтер активируется через файл .golangci.yml. Убедитесь, что в корне проекта созданы два файла:

.custom-gcl.yml (описывает сборку плагина):

```yml
version: v1.62.0
plugins:
  - module: 'loglinter'
    path: '.'
    import: 'loglinter/plugin'
```

.golangci.yml (настройки анализа):

```yaml
linters:
  disable-all: true
  enable:
    - loglinter

linters-settings:
  custom:
    loglinter:
      type: "module"
      description: "checks log messages for style guide compliance"
```
4. Запуск анализа

Запустите проверку вашего кода (например, файла demo.go):

```sh
./custom-gcl run demo.go
```

5. Тестирование

Для запуска внутренних unit-тестов анализатора используйте стандартную команду Go:

```sh
go test ./pkg/analyzer/...
```

Примеры срабатываний

// ❌ Неправильно
```go
slog.Info("Starting server")       // Ошибка: начинается с большой буквы
```
```go
slog.Error("ошибка базы")          // Ошибка: не английский язык
```
```go
slog.Warn("connection failed!")    // Ошибка: знак препинания в конце
```
```go
slog.Debug("api_key: " + key)      // Ошибка: чувствительные данные
```
```go
slog.Info("done 🚀")               // Ошибка: использование эмодзи
```

// ✅ Правильно
```go
slog.Info("starting server")
```
```go
slog.Error("database error")
```
```go
slog.Warn("connection failed")
```
```go
slog.Info("operation completed")
```

**Лицензия**

**MIT**